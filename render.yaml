services:
  - type: web
    name: to-do-lists
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.web:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
